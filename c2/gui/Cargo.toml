[package]
name = "c2-gui"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "1", features = ["shell-all"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
protocol = { path = "../protocol" }
log = "0.4"

[build-dependencies]
tauri-build = "1"
