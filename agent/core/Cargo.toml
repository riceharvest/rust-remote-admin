[package]
name = "agent-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
protocol = { path = "../../protocol" }
agent_modules = { path = "../modules", package = "agent-modules" }
agent_hardening = { path = "../hardening", package = "agent-hardening" }
