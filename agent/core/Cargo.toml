[package]
name = "agent-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
protocol = { path = "../protocol" }
modules = { path = "../modules" }
hardening = { path = "../hardening" }
